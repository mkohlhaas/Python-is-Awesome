#!/usr/bin/env python

# from math import cos, sin, pi
# Don't use wildcard imports
from math import *

if __name__ == "__main__":
    str = "My String"
    print(str)

    # Built-in function 'str' has been overridden
    # y = 10
    # z = str(y)

    a = 42
    b = sin(pi / 2)
    print(b)
    c = cos(pi / 2)
